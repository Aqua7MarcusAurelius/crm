# TODO и планы

## 🔴 Высокий приоритет

- [ ] **Seed данные** — заполнить проекты, должности, корп. email (выпадающие списки пустые)
- [ ] **Пермишены в БД** — создать записи permissions, назначить пользователям
- [ ] **Nginx** — реверс-прокси для фронта и бэка
- [ ] **Домен + SSL** — покупка домена, Let's Encrypt, субдомены (crm.domain, pass.domain, cloud.domain)

## 🟡 Средний приоритет

- [ ] **Файловое хранилище** — интеграция MinIO: загрузка, привязка к сущностям по ID
- [ ] **Чат** — переписки между сотрудниками (Socket.io уже установлен)
- [ ] **Тикеты** — система тикетов с чатом внутри
- [ ] **Email уведомления** — Nodemailer + SMTP настройка
- [ ] **BullMQ очереди** — фоновые задачи, напоминания
- [ ] **Логирование** — подключить Pino к NestJS

## 🟢 Низкий приоритет

- [ ] **Полнотекстовый поиск** — PostgreSQL FTS по клиентам
- [ ] **Тесты** — Vitest (юнит), Playwright (e2e)
- [ ] **Мониторинг** — Sentry для ошибок
- [ ] **CI/CD** — GitHub Actions (тесты + деплой)
- [ ] **Telegram-бот** — уведомления через Bot API

## 💡 Идеи на будущее

- Nextcloud (cloud.domain) — облачное хранилище
- Passbolt (pass.domain) — менеджер паролей
- Дашборд с аналитикой (графики, воронки)
- Экспорт клиентов в CSV/Excel
- Массовые действия над клиентами (выделить → удалить/изменить)
- Фильтры клиентов по стране, статусу, дате
- История изменений клиента (audit log)

## ⚠️ Технические заметки

1. Prisma 7 — URL в `prisma.config.ts`, не в schema.prisma
2. Zod 4 — `{ error: '...' }` вместо `{ required_error: '...' }`
3. SSH Ubuntu 24.04 — порт через `systemctl edit ssh.socket`
4. Два .env — `~/crm/.env` (Docker) и `~/crm/backend/.env` (Prisma + JWT)
5. Запуск только через `start.sh` (ручной запуск → конфликты портов)
6. После `prisma migrate dev` нужен `prisma generate` если TS не видит новые модели
7. Убивать процессы: `pkill -f "nest start"`, не `kill $(lsof -t -i:3000)` (убьёт VS Code)
